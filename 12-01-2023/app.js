import createTodo from './utils/createTodo.js';

// todos.forEach((item) => console.log(item.id));

// const userId = todos.map((item) => item.userId);
// console.log(userId);

// const filterTodo = todos.filter((item) => item.id >= 4);
// console.log(filterTodo);

// Todo =)

createTodo();
